import { Hammersmith_One, Kablammo, Lilita_One, Open_Sans, Press_Start_2P } from 'next/font/google';

export const hammersmithOne = Hammersmith_One({
  subsets: ['latin', 'latin-ext'],
  weight: "400",
});

export const kablammo = Kablammo({
  subsets: ['latin', 'latin-ext'],
});

export const lilitaOne = Lilita_One({
  subsets: ['latin', 'latin-ext'],
  weight: "400",
});

export const openSans = Open_Sans({
  subsets: ['latin', 'latin-ext'],
});

export const pressStart2P = Press_Start_2P({
  subsets: ['latin', 'latin-ext'],
  weight: "400",
});